cluster.master.IP = ${master.ip}
cluster.master.jms.port=${master.jms.port}


cluster.local.jms.port=${local.jms.port}
cluster.local.is.master = ${local.is.master}
cluster.local.ip=${local.ip}
cluster.local.jms.brokername= ${local.jms.brokername}

#Used only in camel context(so it needs the variables from pom!!!
#cluster.local.jetty.port=${local.jetty.port}
#cluster.local.websocket.port=${local.websocket.port}

#61616